spring:
  application:
    name: ticket-server
  jpa:
    generate-ddl: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
  etcd:
    client:
      endpoints:
        - http://127.0.0.1:2379
server:
  port: 8090