spring:
  application:
    name: ticket-server
  datasource:
    url: jdbc:h2:mem:siberian_sea_battle
    driver-class-name: org.h2.Driver
    username: username
    password: password
  jpa:
    generate-ddl: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always
  etcd:
    client:
      endpoints:
        - http://172.17.0.1:2379
server:
  port: 8090